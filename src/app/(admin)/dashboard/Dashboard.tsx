'use client';

import { ReactElement } from 'react';

export const Dashboard = (): ReactElement => {
  // show business hours, break times
  // team member count
  // active services count
  // customer bookings count (datepicker for: this week, this month, this year)
  return <div>Dashboard </div>;
};
